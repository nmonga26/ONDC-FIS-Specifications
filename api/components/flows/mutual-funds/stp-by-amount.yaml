summary: STP - by amount

references: if any
steps:
  - summary: Select stp in a particular folio and scheme
    api: select
    details:
      - description: Send order details and folio details
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/select/switch/select-stp.json"
  - summary: Get the acknowledgement back
    api: on_select
    details:
      - description: Acknowledgement is sent back
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_select/switch/on_select-stp.json"

  - summary: Initialize the stp order
    api: init
    details:
      - description: Send order details and folio details
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/init/switch/init-stp.json"
  - summary: On Init
    api: on_init
    details:
      - description: Draft order is sent back in created state
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_init/switch/on_init-stp.json"

  - summary: Confirm
    api: confirm
    details:
      - description: Send order confirmation w/ 2fa details
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/confirm/switch/confirm-stp.json"
  - summary: On Confirm
    api: on_confirm
    details:
      - description: Send accepted order
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_confirm/switch/on_confirm-stp.json"
