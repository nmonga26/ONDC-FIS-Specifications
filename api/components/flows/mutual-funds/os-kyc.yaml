summary: Other Services - New KYC
details:
  - description: This is showing independent kyc service flow. Select and Init calls are similar to zero balance folio flow except that the xinput will be multiple - hence now showing in this example

references: if any
steps:
  - summary: Confirm
    api: confirm
    details:
      - description: Send confirmation to perform kyc
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/confirm/other_services/confirm-kyc.json"
  - summary: On Confirm
    api: on_confirm
    details:
      - description: Receive acknowledgement that order is accepted and the kyc will be processed
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_confirm/other_services/on_confirm-kyc.json"

  - summary: On Update
    api: on_update
    details:
      - description: Receive notification that the kyc is processed successfully
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_update/other_services/on_update-kyc-success.json"

