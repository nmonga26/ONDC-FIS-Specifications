summary: NCT - New KYC

references: if any
steps:
  # - summary: Confirm
  #   api: confirm
  #   details:
  #     - description: Send confirmation to open folio
  #   reference: if any
  #   example:
  #     value:
  #       $ref: "../../examples/mutual-funds/confirm/confirm-kyc.json"
  - summary: On Confirm
    api: on_confirm
    details:
      - description: Receive acknowledgement that order is accepted and the kyc will be processed
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_confirm/on_confirm-kyc.json"

  - summary: On Update
    api: on_update
    details:
      - description: Receive notification that the kyc is processed successfully
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_update/on_update-kyc-success.json"

