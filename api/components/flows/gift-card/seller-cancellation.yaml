summary: Seller Cancellation
details:           
references: if any
steps:
  - summary: Provider confirms the order
    api: on_confirm
    details:
      - description: Provider confirms the order
        mermaid: >-
          sequenceDiagram
            title  Order Confirmation
            participant Buyer Platform (BAP)
            participant Provider Platform (BPP)
            rect rgb(191, 223, 255)
            Provider Platform (BPP)->>Buyer Platform (BAP):on_confirm
            Buyer Platform (BAP)-->>Provider Platform (BPP): ACK 
            end
            Provider Platform (BPP)->>Buyer Platform (BAP):on_cancel
            Buyer Platform (BAP)-->>Provider Platform (BPP): ACK 
    reference: if any
    example:
      value:
        $ref: "../../examples/gift-card/on_confirm/on_confirm_request.yaml"
  - summary: Seller app not able to deliver the gift card
    api: on_cancel
    details:
      - description: In case the seller app is not able to deliver the gift card to the details (phone or email or both) provided by the buyer, the seller may either initiate order cancellation or may ask the buyer to provide the correct details
        mermaid: >-
          sequenceDiagram
            title  Order Confirmation
            participant Buyer Platform (BAP)
            participant Provider Platform (BPP)
            Provider Platform (BPP)->>Buyer Platform (BAP):on_confirm
            Buyer Platform (BAP)-->>Provider Platform (BPP): ACK 
            rect rgb(191, 223, 255)
            Provider Platform (BPP)->>Buyer Platform (BAP):on_cancel
            Buyer Platform (BAP)-->>Provider Platform (BPP): ACK 
            end
    reference: if any
    example:
      value:
        $ref: "../../examples/gift-card/on_cancel/on_cancel_request.yaml"        
  